#! /bin/bash

CUR_GW=$( curl -s http://169.254.169.254/metadata/v1/interfaces/public/0/ipv4/gateway )
echo "$CUR_GW" >> /tmp/curgw
ip route add 169.254.169.254 via $CUR_GW dev eth0
ip route change default via {{ gateway_ip }}

# finish command
touch /tmp/setup-route-do.sh.done
